<Window x:Class="ComfyDeployControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Comfy Deploy Control" Height="900" Width="1360"
        MinHeight="760" MinWidth="1120"
        WindowStartupLocation="CenterScreen"
        Background="#0A0D14"
        Loaded="Window_Loaded"
        Closing="Window_Closing">
    <Window.Resources>
        <Style x:Key="Panel" TargetType="Border">
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#294356"/>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#121821" Offset="0"/>
                        <GradientStop Color="#101B20" Offset="0.55"/>
                        <GradientStop Color="#1A1720" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="18"/>
            <Setter Property="Margin" Value="0,0,14,14"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#05070A" BlurRadius="18" ShadowDepth="4" Opacity="0.32"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SoftButton" TargetType="Button">
            <Setter Property="Foreground" Value="#F4F7FB"/>
            <Setter Property="Background" Value="#17212C"/>
            <Setter Property="BorderBrush" Value="#3C6276"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="Margin" Value="0,0,10,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#1B2D34" Offset="0"/>
                                    <GradientStop Color="#20314A" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform/>
                                    <TranslateTransform/>
                                </TransformGroup>
                            </Border.RenderTransform>
                            <Grid>
                                <Rectangle Width="3" HorizontalAlignment="Left" RadiusX="2" RadiusY="2" Fill="#79D6C1" Opacity="0.75" Margin="-5,1,8,1"/>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="8,0,0,0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="#79D6C1"/>
                                <Setter TargetName="Root" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#35B8A8" BlurRadius="16" ShadowDepth="0" Opacity="0.35"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.76"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource SoftButton}">
            <Setter Property="Background" Value="#D66A35"/>
            <Setter Property="BorderBrush" Value="#F0B35A"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#0E1320"/>
            <Setter Property="Foreground" Value="#EDF2F7"/>
            <Setter Property="CaretBrush" Value="#F09A62"/>
            <Setter Property="BorderBrush" Value="#2E3A4B"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,4,0,12"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#0E1320"/>
            <Setter Property="Foreground" Value="#EDF2F7"/>
            <Setter Property="CaretBrush" Value="#F09A62"/>
            <Setter Property="BorderBrush" Value="#2E3A4B"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,4,0,12"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#0E1320"/>
            <Setter Property="Foreground" Value="#EDF2F7"/>
            <Setter Property="BorderBrush" Value="#2E3A4B"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="0,4,0,12"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#D7DEE8"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#070B10" Offset="0"/>
                <GradientStop Color="#0A1F24" Offset="0.42"/>
                <GradientStop Color="#211720" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="88"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="34"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="28,18" BorderBrush="#223044" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Comfy Control Deck" FontSize="32" FontWeight="Black" Foreground="#FFFFFF"/>
                    <TextBlock Text="Install in ComfyUI, bake to Docker, redeploy Modal, and keep the logs moving."
                               Foreground="#B7C8D8" Margin="0,4,0,0"/>
                </StackPanel>
                <Border Grid.Column="1" CornerRadius="6" Padding="14,10" Background="#111827" BorderBrush="#304157" BorderThickness="1">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse x:Name="PulseDot" Width="10" Height="10" Fill="#F0B35A" Margin="0,0,10,0">
                            <Ellipse.Triggers>
                                <EventTrigger RoutedEvent="Loaded">
                                    <BeginStoryboard>
                                        <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.35" To="1" Duration="0:0:1.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Ellipse.Triggers>
                        </Ellipse>
                        <TextBlock x:Name="StatusText" Text="Idle" FontWeight="SemiBold" Foreground="#F6D08A"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="24,20,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Border Style="{StaticResource Panel}">
                        <StackPanel>
                            <TextBlock Text="Access" FontSize="18" FontWeight="Bold" Foreground="#FFFFFF"/>
                            <TextBlock Text="GitHub uses the installed gh CLI. DockerHub secrets are sent to GitHub Actions and can also prune old sha tags."
                                       TextWrapping="Wrap" Foreground="#9EADBF" Margin="0,6,0,12"/>

                            <TextBlock Text="GitHub owner"/>
                            <TextBox x:Name="GitHubOwnerBox"/>
                            <TextBlock Text="GitHub repo"/>
                            <TextBox x:Name="GitHubRepoBox"/>
                            <TextBlock Text="Local repo path"/>
                            <TextBox x:Name="RepoPathBox"/>

                            <TextBlock Text="DockerHub username"/>
                            <TextBox x:Name="DockerUsernameBox"/>
                            <TextBlock Text="DockerHub token"/>
                            <PasswordBox x:Name="DockerTokenBox"/>
                            <TextBlock Text="Docker namespace / repo"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="DockerNamespaceBox"/>
                                <TextBox Grid.Column="2" x:Name="DockerRepoBox"/>
                            </Grid>

                            <StackPanel Orientation="Horizontal">
                                <Button Style="{StaticResource SoftButton}" Content="Save" Click="SaveSettings_Click"/>
                                <Button Style="{StaticResource SoftButton}" Content="Check access" Click="CheckAccess_Click"/>
                            </StackPanel>
                            <Button Style="{StaticResource PrimaryButton}" Content="Configure GitHub secrets" Click="ConfigureSecrets_Click"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource Panel}">
                        <StackPanel>
                            <TextBlock Text="Modal App" FontSize="18" FontWeight="Bold" Foreground="#FFFFFF"/>
                            <TextBlock Text="App name"/>
                            <TextBox x:Name="ModalAppBox"/>
                            <TextBlock Text="Volume name"/>
                            <TextBox x:Name="ModalVolumeBox"/>
                            <TextBlock Text="GPU"/>
                            <ComboBox x:Name="GpuBox">
                                <ComboBoxItem Content="A10"/>
                                <ComboBoxItem Content="L40S"/>
                                <ComboBoxItem Content="A100"/>
                                <ComboBoxItem Content="H100"/>
                            </ComboBox>

                            <StackPanel Orientation="Horizontal">
                                <Button Style="{StaticResource SoftButton}" Content="Deploy" Click="Deploy_Click"/>
                                <Button Style="{StaticResource SoftButton}" Content="Stop app" Click="StopApp_Click"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <Button Style="{StaticResource SoftButton}" Content="Open ComfyUI" Click="OpenComfy_Click"/>
                                <Button Style="{StaticResource SoftButton}" Content="Open Jupyter" Click="OpenJupyter_Click"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource Panel}">
                        <StackPanel>
                            <TextBlock Text="Automation" FontSize="18" FontWeight="Bold" Foreground="#FFFFFF"/>
                            <CheckBox x:Name="AutoWatchBox" Content="Watch Modal custom nodes and rebuild on changes"
                                      Foreground="#D7DEE8" Margin="0,10,0,10" Checked="AutoWatch_Changed" Unchecked="AutoWatch_Changed"/>
                            <CheckBox x:Name="PruneTagsBox" Content="Delete old DockerHub sha tags after a successful build"
                                      Foreground="#D7DEE8" Margin="0,0,0,12"/>
                            <TextBlock Text="Keep sha tags"/>
                            <TextBox x:Name="KeepTagsBox" Text="3"/>
                            <Button Style="{StaticResource PrimaryButton}" Content="Sync nodes, build, deploy" Click="RunPipeline_Click"/>
                            <Button Style="{StaticResource SoftButton}" Content="Force build from current repo" Click="ForceBuild_Click"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <UniformGrid Grid.Row="0" Columns="4" Margin="0,0,14,8">
                    <Border Style="{StaticResource Panel}">
                        <StackPanel>
                            <TextBlock Text="Nodes" Foreground="#91D6B4"/>
                            <TextBlock x:Name="NodeCountText" Text="-" FontSize="28" FontWeight="Bold" Foreground="#FFFFFF"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource Panel}">
                        <StackPanel>
                            <TextBlock Text="Build" Foreground="#F0B35A"/>
                            <TextBlock x:Name="BuildStatusText" Text="Unknown" FontSize="20" FontWeight="Bold" Foreground="#FFFFFF"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource Panel}">
                        <StackPanel>
                            <TextBlock Text="Docker image" Foreground="#8AC7E8"/>
                            <TextBlock x:Name="ImageText" Text="anqipudding/modal_comfyui:latest" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource Panel}">
                        <StackPanel>
                            <TextBlock Text="Watcher" Foreground="#E78F8E"/>
                            <TextBlock x:Name="WatcherText" Text="Off" FontSize="20" FontWeight="Bold" Foreground="#FFFFFF"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <TabControl Grid.Row="1" Margin="0,0,14,14" Background="#101521" BorderBrush="#263244">
                    <TabItem Header="Pipeline">
                        <Grid Background="#101521">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="320"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Margin="14" Padding="14" Background="#151A24" BorderBrush="#263244" BorderThickness="1" CornerRadius="8">
                                <DockPanel>
                                    <TextBlock DockPanel.Dock="Top" Text="Custom nodes seen in Modal" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,10"/>
                                    <ListBox x:Name="NodeList" Background="#0E1320" Foreground="#D7DEE8" BorderBrush="#2E3A4B"/>
                                </DockPanel>
                            </Border>
                            <Border Grid.Column="1" Margin="0,14,14,14" Padding="14" Background="#0E1320" BorderBrush="#263244" BorderThickness="1" CornerRadius="8">
                                <TextBox x:Name="PipelineLogBox" Background="#0A0D14" Foreground="#CDE3D5" FontFamily="Consolas"
                                         BorderThickness="0" IsReadOnly="True" TextWrapping="Wrap"
                                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
                            </Border>
                        </Grid>
                    </TabItem>
                    <TabItem Header="Modal logs">
                        <DockPanel Background="#101521" Margin="14">
                            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                                <Button Style="{StaticResource SoftButton}" Content="Start app logs" Click="StartAppLogs_Click"/>
                                <Button Style="{StaticResource SoftButton}" Content="Stop logs" Click="StopLogs_Click"/>
                            </StackPanel>
                            <TextBox x:Name="ModalLogBox" Background="#0A0D14" Foreground="#D6E4FF" FontFamily="Consolas"
                                     BorderThickness="0" IsReadOnly="True" TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
                        </DockPanel>
                    </TabItem>
                    <TabItem Header="ComfyUI logs">
                        <DockPanel Background="#101521" Margin="14">
                            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                                <Button Style="{StaticResource SoftButton}" Content="Start ComfyUI logs" Click="StartComfyLogs_Click"/>
                                <Button Style="{StaticResource SoftButton}" Content="Stop logs" Click="StopLogs_Click"/>
                            </StackPanel>
                            <TextBox x:Name="ComfyLogBox" Background="#0A0D14" Foreground="#F9E8C9" FontFamily="Consolas"
                                     BorderThickness="0" IsReadOnly="True" TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
                        </DockPanel>
                    </TabItem>
                    <TabItem Header="Build history">
                        <DockPanel Background="#101521" Margin="14">
                            <Button DockPanel.Dock="Top" Style="{StaticResource SoftButton}" Content="Refresh build history" HorizontalAlignment="Left" Click="RefreshBuilds_Click"/>
                            <TextBox x:Name="BuildHistoryBox" Background="#0A0D14" Foreground="#FFFFFF" FontFamily="Consolas"
                                     BorderThickness="0" IsReadOnly="True" TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
                        </DockPanel>
                    </TabItem>
                </TabControl>
            </Grid>
        </Grid>

        <TextBlock Grid.Row="2" Text="Tip: keep models in /data/models; keep custom node folders code-only so image rebuilds stay fast."
                   Foreground="#8FA2B8" Margin="28,0,0,10" VerticalAlignment="Center"/>
    </Grid>
</Window>
